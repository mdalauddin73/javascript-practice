// All in One ternary (three parts) conditional operator
const age = 17;
age >= 18 ? console.log('I can drink') : console.log('I cannot drink');

// simplify the above expression
age >= 18 ? 'wine' : 'water';

const result = age >= 18 ? 'wine' : 'water';
console.log(result);

// operation always produces vales, and operator is an expresions